# Deploy on Render

In your Render service **Settings → Build & Deploy**:

- **Root Directory:** `chatbot/backend`  
  (Use this exact path. Do not use `backend` alone — the backend lives inside the `chatbot` folder.)

Then save and redeploy.
