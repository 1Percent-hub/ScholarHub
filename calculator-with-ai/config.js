/**
 * API URL for the Creater AI widget.
 * - For local testing with START_WITH_AI.bat: leave as null (script will use localhost).
 * - For a live website: set this to your deployed backend URL (no trailing slash).
 *   Example: window.CHATBOT_API_URL = "https://your-app.onrender.com/api/chat";
 */
window.CHATBOT_API_URL = "https://scholarhub-api.onrender.com/api/chat";
